# Core Dependencies
requests>=2.31.0      # HTTP requests (CoinMarketCap API, Binance downloads)
aiohttp>=3.9.0        # Async HTTP requests (parallel Binance downloads)
PyYAML>=6.0           # Config file parsing (YAML)
pandas>=2.0.0         # DataFrame operations (data ingestion)
duckdb>=0.9.0         # Embedded database engine
pyarrow>=19.0.0       # Parquet file support (used by DuckDB)